name: Verify UptimeBond

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            requirements-dev.txt
            requirements-deploy.txt
      - run: python -m pip install -r requirements-dev.txt
      - name: Cache verified GenVM runner bundle
        run: python scripts/cache_genvm_runners.py
      - run: genvm-lint check contracts/uptime_bond.py
      - run: python -m pytest tests/direct/test_uptime_bond.py -q
      - run: python -m py_compile scripts/cache_genvm_runners.py scripts/deploy_uptime_bond.py scripts/uptimebond_acceptance.py tests/integration/test_uptime_bond_studionet.py scripts/check_uptime_bond_release.py

  web:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/uptimebond-web
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
        with:
          version: 11.19.0
      - uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: pnpm
          cache-dependency-path: apps/uptimebond-web/pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
      - run: pnpm test
      - run: pnpm typecheck
      - run: pnpm build
      - run: pnpm audit:prod
